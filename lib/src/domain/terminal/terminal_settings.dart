/// Terminal settings configuration.
///
/// Contains settings for terminal behavior including receipts,
/// connectivity, gratuities, and hardware configuration.
class TerminalSettings {
  /// Cardholder receipt settings.
  dynamic cardholderReceipt;

  /// Connectivity settings.
  dynamic connectivity;

  /// Gratuity settings.
  dynamic gratuities;

  /// Hardware settings.
  dynamic hardware;
}
